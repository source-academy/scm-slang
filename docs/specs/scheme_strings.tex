\section{Strings}

Strings are delimited by double quotes and evaluate to immutable string values.
The standard library provides operations such as \texttt{string},
\texttt{string-length}, \texttt{string-ref}, \texttt{substring},
\texttt{string-append}, \texttt{string->number} and \texttt{number->string}.
